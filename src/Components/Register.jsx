import React from 'react'
import {
    Input,
    Ripple,
    initTE,
} from "tw-elements";
const Register = () => {
    initTE({ Input, Ripple });
    return (
        <>
            
        </>
    )
}

export default Register